server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    # Serve the file if it exists; otherwise fall back to the SPA entry.
    try_files $uri $uri/ /index.html;
  }

  location /api/ {
    proxy_pass http://backend:4000$request_uri;
  }
}
